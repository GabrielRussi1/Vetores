#include <stdio.h>

int main() {
    int vetor[5] = {10, 20, 30, 40, 50}; // Inicializando um vetor de 5 inteiros
    int i;

    // Percorrendo o vetor e exibindo seus valores
    for (i = 0; i < 5; i++) {
        printf("vetor[%d] = %d\n", i, vetor[i]);
    }

    return 0;
}
